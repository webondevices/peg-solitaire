import React from 'react';

class Peg extends React.Component {
    render () {
        if (this.props.peg === null) {
            return (<div></div>);
        } else {
            return (<div></div>);
        }
    }
}

export default Peg;